# ChatGPT Sidebar Organizer 使用手册

[English](manual.md) | **简体中文**

本手册适用于 **2.2.0** 版本。

英文版和简体中文版 userscript 的功能完全一致，并共用同一套本地数据。请始终只安装并启用其中一个语言版本。

## 1. Organizer 会保存什么

Organizer 只在当前浏览器中保存整理所需的元数据：

- 会话标题和 URL
- 分区归属与分区颜色
- 可选状态标签
- 会话和分区的自定义顺序
- 窗口位置、筛选设置和分区折叠状态
- 从某个分区创建新会话时所需的临时信息

Organizer 不会复制或保存聊天正文。这些数据也不会在不同浏览器或设备之间自动同步。

## 2. Organizer 窗口

Organizer 是一个可移动的小窗口，因此不必固定遮挡 ChatGPT 原生侧边栏。

- 拖动 **Chat Organizer** 左侧的 `⠿` 可以移动窗口。
- 点击 `×` 可以关闭面板，页面中会保留一个小型 **Organizer** 启动按钮。
- 点击 `↑` 可以回到会话列表顶部。
- 点击 `▾` 可以展开所有分区。
- 点击 `▴` 可以收起所有分区。

左侧目录只显示分区颜色和名称。点击名称可以快速跳转到对应分区。为保持目录紧凑，目录中不会显示每个分区的会话数量。

## 3. 创建和管理分区

点击顶部的 **分区**。

### 创建分区

1. 点击 **新建分区**。
2. 输入分区名称。
3. 从 12 个预设颜色中选择，或使用自由取色器。
4. 点击 **创建**。

忽略大小写和首尾空格后，分区名称不能重复。

### 编辑或调整分区顺序

- 在“管理分区”中直接修改名称或颜色。
- 拖动 `⠿` 手柄改变分区顺序。
- 点击 **完成** 保存文字和颜色修改。

### 删除分区

- 点击某个分区右侧的 `×` 删除该分区。
- 点击 **删除所有分区** 一次移除全部分区。

删除分区不会删除 ChatGPT 会话。原本位于其中的会话会回到“未分区”。

## 4. 为会话设置分区和状态

打开一个已经保存的 ChatGPT 会话后，Organizer 顶部会显示“当前聊天”卡片。

点击该卡片，或点击任意已索引会话右侧的 `•••`，即可打开“整理聊天”。你可以：

- 指定一个分区，或将会话设为“未分区”。
- 设置 `Active`、`Reference`、`Backlog` 或“未设置”。
- 不离开编辑窗口，直接创建新分区。
- 只从 Organizer 的本地索引中移除该会话。

分区和状态彼此独立。会话可以有分区但没有状态，也可以在未分区时设置状态，或者同时使用两者。

Organizer 不会另外保存一个本地备注名。显示的名称始终以 ChatGPT 中的真实会话标题为准。

## 5. 状态标签

- **未设置**：不显示状态标签。
- **Active**：当前正在使用或推进。
- **Reference**：主要作为以后查阅的参考资料。
- **Backlog**：暂时搁置、以后再处理；不会隐藏会话。

旧版本备份中的 `Hidden` 会自动迁移为 `Backlog`。

## 6. 索引会话

Organizer 会自动识别 ChatGPT 原生导航中当前可见的会话链接，也会记录你实际打开过的会话。

点击 **重新索引** 可以进行更完整的扫描。Organizer 会：

1. 寻找可滚动的原生历史列表。
2. 移动到列表顶部。
3. 分段滚动 ChatGPT 的虚拟列表。
4. 收集每一步加载出来的有效会话链接和标题。
5. 在条件允许时恢复原来的滚动位置。

重新索引不能绕过 ChatGPT 自身的历史记录限制。如果某个很早的会话没有被原生界面加载，可以先通过 ChatGPT 搜索找到并打开一次，之后 Organizer 就能记录它。

脚本会排除“开启桌面应用程式”等导航入口、`Untitled chat` 等临时占位标题，以及不符合会话 ID 格式的链接。

## 7. 专案中的会话

位于 ChatGPT 专案中的会话不会显示在 Organizer 中。

如果一个已经分区的会话被加入专案，Organizer 会保留原来的分区记忆。将它移出专案后，会自动恢复原分区；如果没有有效的历史分区信息，则恢复为“未分区”。

## 8. 直接在分区中创建新会话

每个分区标题右侧都有 `＋`。

1. 点击目标分区的 `＋`。
2. Organizer 会打开一个新的 ChatGPT 会话。
3. 发送第一条消息。
4. 当 ChatGPT 生成正式的会话 URL 后，Organizer 会把它自动加入刚才选择的分区。

待处理的分区指定会在 30 分钟后失效。在发送第一条消息前，也可以通过顶部提示取消。

## 9. 搜索和筛选

搜索框只匹配已索引的会话标题。

点击 **筛选** 可以组合以下条件：

- 分区：全部分区、未分区或某个具体分区
- 状态：全部状态、未设置、Active、Reference 或 Backlog
- 类型：全部、Chat、工作或 GPT
- 排序：自定义拖动排序、最近访问、最早访问或标题 A–Z

点击 **清除筛选** 可以恢复默认视图。

## 10. 自定义排序

拖动会话左侧的 `⠿`，可以在同一分区内改变顺序。不能通过拖动直接把会话移动到另一个分区；跨分区移动请使用会话编辑窗口。

如果当前正在使用其他排序方式，开始拖动时，Organizer 会先把当前可见顺序保存为自定义顺序，并自动切换回“自定义拖动排序”。

## 11. 批量操作

点击 **批量** 可以同时整理多个会话。批量列表只显示当前未分区、且不在专案中的会话。

1. 根据需要在批量窗口中搜索。
2. 单独勾选会话，或点击 **选择当前结果**。
3. 选择分区、状态，或者同时选择两者。
4. 点击 **应用**。

设为“保持不变”的项目不会被修改。会话被指定分区后，下次打开批量窗口时就不会再出现在列表中。

## 12. 重命名和删除

每个会话行右侧提供：

- `✎`：修改 ChatGPT 中的真实会话标题。
- `×`：确认后删除 ChatGPT 中的真实会话。
- `•••`：只编辑 Organizer 的分区和状态。

重命名和删除通过会话 ID 与当前已登录的 ChatGPT 网页会话执行，不要求目标会话当前显示在原生历史侧栏中。

通过 Organizer 删除会话无法撤销。“从本地索引移除”是不同的操作：它不会删除 ChatGPT 会话，而且该会话以后可能再次被索引。

如果会话在其他位置被删除，Organizer 会在检测到原生删除确认，或者发现链接指向不存在的会话后，尝试清除失效的本地记录。

## 13. 备份和恢复

打开 **⚙ 设置与备份**。

- **导出 JSON 备份**：下载全部 Organizer 元数据。
- **导入 JSON**：确认后用备份替换当前 Organizer 数据。
- **清空 Organizer 数据**：重置分区、状态、索引、顺序和界面设置，但不会删除 ChatGPT 会话。

导入的数据会先经过标准化处理。无效 URL、畸形记录、重复分区 ID、不支持的状态和失效引用会被修复或移除。为避免意外卡死浏览器，导入上限为 5 MB、500 个分区和 20,000 个会话。

## 14. 故障排查

### 重新索引找不到历史列表

展开 ChatGPT 原生侧边栏，确认历史记录可见，然后重试。即使找不到可滚动区域，Organizer 仍会收集当前页面上可见的会话链接。

### 标题暂时显示“正在获取标题…”

ChatGPT 有时只会先提供临时占位标题。原生侧边栏或浏览器标题出现真实名称后，Organizer 会自动更新。

### 重命名或删除失败

刷新 ChatGPT，并确认登录了正确账号。ChatGPT 也可能已经更改内部接口；如果确认是可复现的功能回归，可按照项目的 issue 原则反馈。

### 清理浏览器数据后 Organizer 数据消失

Organizer 使用网站本地存储。请导入之前导出的 JSON 备份。

### 浏览器提示无法保存数据

浏览器可能禁用了 ChatGPT 网站存储，或者可用空间已经耗尽。请尽可能先导出备份、移除不需要的索引记录，并检查浏览器的网站存储设置。

## 15. 性能行为

- 脚本不使用轮询定时器。
- 使用一个 MutationObserver 监听页面导航和原生侧边栏替换。
- DOM 触发的扫描和搜索渲染都经过防抖。
- 手动完整索引会集中保存数据，不会在每个滚动步骤后重写全部本地状态。
- 弹窗专用监听器会随弹窗 DOM 一起销毁，不会因为反复打开弹窗而累积。

测试结果、性能测量和暂未采用的高风险优化详见 [AUDIT_REPORT.md](AUDIT_REPORT.md)。
